# Руководство по запуску архитектурных тестов

## 🔧 Требования

- .NET SDK 8.0.411 или совместимая версия (8.0.x)
- Все зависимости проекта должны быть восстановлены

## 🚀 Запуск тестов

### 1. Проверка версии SDK

```bash
dotnet --version
```

Если версия не 8.0.411, у вас есть два варианта:

**Вариант A:** Установить точную версию 8.0.411
```bash
# Скачать с https://dotnet.microsoft.com/download/dotnet/8.0
```

**Вариант B:** Изменить `global.json` для использования доступной версии
```json
{
  "sdk": {
    "rollForward": "latestPatch",
    "version": "8.0.0"
  }
}
```

### 2. Восстановление зависимостей

```bash
dotnet restore tests/ArchitectureTests.TaskForge/ArchitectureTests.TaskForge.csproj
```

### 3. Сборка проекта

```bash
dotnet build tests/ArchitectureTests.TaskForge/ArchitectureTests.TaskForge.csproj
```

### 4. Запуск всех тестов

```bash
dotnet test tests/ArchitectureTests.TaskForge/ArchitectureTests.TaskForge.csproj --verbosity normal
```

### 5. Запуск конкретных категорий тестов

```bash
# Тесты зависимостей слоев
dotnet test tests/ArchitectureTests.TaskForge --filter "FullyQualifiedName~LayerDependencyTests"

# Тесты async паттернов
dotnet test tests/ArchitectureTests.TaskForge --filter "FullyQualifiedName~AsyncPatternTests"

# Тесты безопасности
dotnet test tests/ArchitectureTests.TaskForge --filter "FullyQualifiedName~SecurityTests"
```

### 6. Запуск с подробным выводом

```bash
dotnet test tests/ArchitectureTests.TaskForge/ArchitectureTests.TaskForge.csproj --verbosity detailed
```

## 🔍 Поиск и исправление ошибок

### Типичные проблемы

1. **SDK версия не найдена**
   - Установите .NET SDK 8.0.411 или измените `global.json`

2. **Ошибки компиляции**
   - Проверьте, что все using директивы на месте
   - Убедитесь, что все проекты успешно собираются

3. **Тесты падают**
   - Проверьте вывод тестов для деталей
   - Используйте `--verbosity detailed` для подробной информации

### Структура тестов

- **Core/** - Базовые компоненты (переиспользуемые)
- **Helpers/** - Хелперы для проверок
- **Categories/** - Тесты по категориям
  - `LayerDependencyTests.cs` - Тесты зависимостей слоев
  - `AsyncPatternTests.cs` - Тесты async/await паттернов
  - `SecurityTests.cs` - Тесты безопасности

## 📊 Ожидаемые результаты

После успешного запуска вы должны увидеть:

```
Test Run Successful.
Total tests: X
     Passed: X
     Failed: 0
```

Если есть failures, проверьте детали каждого теста для понимания проблем.

## ⚠️ Примечания

- Некоторые тесты могут требовать наличия исходных файлов
- Тесты проверяют архитектурные правила и конвенции кода
- Если тесты падают, это указывает на нарушения архитектурных правил, которые нужно исправить

