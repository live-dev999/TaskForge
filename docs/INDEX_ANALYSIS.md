# Анализ составного индекса для TaskItems

## Текущая ситуация

**Отдельные индексы:**
- `IX_TaskItems_Status` - на поле `Status`
- `IX_TaskItems_CreatedAt` - на поле `CreatedAt`

**Предлагаемый составной индекс:**
- `IX_TaskItems_Status_CreatedAt` - на полях `(Status, CreatedAt)`

## Когда составной индекс полезен?

### ✅ Полезен для запросов:

1. **Фильтрация по Status + Сортировка по CreatedAt:**
   ```sql
   SELECT * FROM TaskItems 
   WHERE Status = 1 
   ORDER BY CreatedAt DESC
   ```
   - Составной индекс полностью покрывает запрос
   - Не нужно отдельно сортировать результаты
   - SQL Server может использовать индекс для фильтрации и сортировки

2. **Фильтрация по Status (левая часть индекса):**
   ```sql
   SELECT * FROM TaskItems WHERE Status = 1
   ```
   - Составной индекс можно использовать (нормально работает)
   - Отдельный индекс на Status также подходит

3. **Диапазоны по обеим полям:**
   ```sql
   SELECT * FROM TaskItems 
   WHERE Status = 1 AND CreatedAt > '2024-01-01'
   ```

### ❌ НЕ полезен для:

1. **Только сортировка по CreatedAt:**
   ```sql
   SELECT * FROM TaskItems ORDER BY CreatedAt
   ```
   - Составной индекс не будет использован эффективно
   - Нужен отдельный индекс на CreatedAt (уже есть)

2. **Запросы без фильтрации по Status:**
   ```sql
   SELECT * FROM TaskItems ORDER BY CreatedAt
   ```
   - Составной индекс не подходит

## Влияние на INSERT/UPDATE

### ⚠️ Небольшое замедление INSERT/UPDATE:

1. **INSERT операция:**
   - Дополнительное время на обновление индекса (~1-2ms)
   - Для небольших таблиц (< 100K записей) влияние минимально
   - Для больших таблиц может быть заметно

2. **UPDATE операция:**
   - Если обновляется Status или CreatedAt → индекс перестраивается
   - Если обновляются другие поля → влияние минимально

3. **Дополнительное место на диске:**
   - Составной индекс занимает больше места, чем отдельные
   - ~20-30% дополнительного пространства

## Рекомендация для текущего проекта

### 🔍 Текущие запросы:
- `List`: `SELECT * FROM TaskItems` - нет фильтрации
- Группировка выполняется на клиенте
- Нет запросов с WHERE Status + ORDER BY CreatedAt

### ✅ Рекомендация: **НЕ добавлять сейчас**

**Причины:**
1. Текущие запросы не используют эту комбинацию
2. Отдельные индексы уже покрывают текущие потребности
3. Преждевременная оптимизация

### ✅ Когда стоит добавить:

**Добавьте составной индекс, если планируете:**
1. Запросы с фильтрацией по статусу и сортировкой по дате:
   ```csharp
   var items = await _context.TaskItems
       .Where(t => t.Status == TaskItemStatus.InProgress)
       .OrderByDescending(t => t.CreatedAt)
       .ToListAsync();
   ```

2. Dashboard с задачами, сгруппированными по статусу и отсортированными по дате

3. Пагинацию с фильтром по статусу

## Альтернатива: добавить при необходимости

Составной индекс можно добавить позже через миграцию, когда появится реальная необходимость. Это не критично для текущей функциональности.

## Итоговая таблица сравнения

| Критерий | Отдельные индексы | Составной индекс |
|----------|-------------------|------------------|
| **Фильтрация по Status** | ✅ Отлично | ✅ Хорошо |
| **Сортировка по CreatedAt** | ✅ Отлично | ❌ Не используется |
| **Status + ORDER BY CreatedAt** | ⚠️ Медленнее | ✅ Отлично |
| **INSERT скорость** | ✅ Быстрее | ⚠️ Немного медленнее |
| **Использование диска** | ✅ Меньше | ⚠️ Больше |
| **Текущая необходимость** | ✅ Есть | ❌ Нет |

## Вывод

Для текущего проекта **НЕ стоит добавлять** составной индекс, так как:
- Текущие запросы его не используют
- Отдельные индексы покрывают текущие потребности
- Можно добавить позже через миграцию при появлении необходимости

